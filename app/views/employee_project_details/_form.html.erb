<%= form_with(model: employee_project_detail) do |form| %>
  <% if employee_project_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee_project_detail.errors.count, "error") %> prohibited this employee_project_detail from being saved:</h2>

      <ul>
        <% employee_project_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :employee %>
    <%= form.text_field :employee %>
  </div>

  <div class="field">
    <%= form.label :project_detail %>
    <%= form.text_field :project_detail %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
